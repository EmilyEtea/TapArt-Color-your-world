// Basic smoke test for TapArt
// TODO: add proper widget tests when may time na haha

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('TapArt smoke test', (WidgetTester tester) async {
    // placeholder lang muna — Firebase requires real init para sa full test
    expect(true, isTrue);
  });
}
